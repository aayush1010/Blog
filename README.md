# Blog

An Angular blog app: log in, write and add new posts, list posts, and filter the list by tags. Backed by a `db.json` fixture (served separately, e.g. via [ServerBlog](https://github.com/aayush1010/ServerBlog)).

**Tech:** Angular, TypeScript

## Run

`ng serve` then open `http://localhost:4200`
